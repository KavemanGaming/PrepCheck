import 'package:flutter/material.dart';
class InventoryListManager extends StatelessWidget {
  const InventoryListManager({super.key});
  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Inventory Manager placeholder')));
}
